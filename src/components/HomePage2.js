import React from "react";
import {MainContainer, ContentContainer, Header} from "../styled_components/HomePage2Styled";

export default function HomePage2() {
  return (
    <MainContainer>
        <ContentContainer>
            <Header>This is header</Header>
        </ContentContainer>
    </MainContainer>
  );
}